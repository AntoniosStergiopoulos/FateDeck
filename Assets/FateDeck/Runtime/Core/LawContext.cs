namespace FateDeck.Runtime.Core
{
    /// <summary>The five law contexts of the GDD; every force entry carries one effect list per context.</summary>
    public enum LawContext
    {
        PlayerOffense,
        PlayerDefense,
        EnemyAction,
        Loot,
        Ritual
    }
}
